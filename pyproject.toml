[project]
name = "spotify-archive"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "spotify>=0.10.2",
]

[dependency-groups]
dev = [
    "ruff>=0.8.4",
]
